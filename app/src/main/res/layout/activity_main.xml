<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.access.accessonandroid.MainActivity">

    <!-- This defines the layout of the main launch activity -->
    <!-- Authors: Tyler Collison, Megan Goins, and Mae Hutchison -->

    <!-- The button used to begin authentication -->
    <Button
        android:id="@+id/authButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:text="Authenticate" />

    <!-- The button used to change the password -->
    <Button
        android:id="@+id/passwordButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/authButton"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/authButton"
        android:layout_marginBottom="65dp"
        android:text="Change Password" />
</RelativeLayout>
