<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.access.accessonandroid.MainActivity">

    <!-- This layout defines instruction for facial recognition -->
    <!-- Author: Mae Hutchison -->

    <!-- Defines a frame divider -->
    <FrameLayout
        android:id="@+id/camera_preview"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"

        android:layout_alignParentTop="true"
        android:layout_marginLeft="23dp"
        android:layout_toRightOf="@+id/floatingActionButton"
        android:visibility="visible">

        <!-- Displays instructions to the user -->
        <TextView
            android:id="@+id/textView"
            android:layout_width="216dp"
            android:layout_height="318dp"
            android:text="How to Use Authenticator
    Step 1. Point the front camera at your face and center your face in the screen.
    Step 2. Place your finger on the fingerprint scanner while keeping the camera pointed at your face.
    Step 3. Wait for emulation " />
    </FrameLayout>

    <!-- Button used to capture an image -->
    <Button
        android:id="@+id/button_capture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Capture"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/floatingActionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:src="@android:drawable/ic_menu_help"
        app:fabSize="mini"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />
    <!--android:onClick="display"-->

</RelativeLayout>


